{
  "name": "My Dev Container Project",
  "postCreateCommand": "sudo apt-get update && sudo DEBIAN_FRONTEND=noninteractive apt-get install -y mariadb-server mariadb-client",
  "postStartCommand": "sudo service mariadb start"
}
